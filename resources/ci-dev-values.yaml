global:
  ingress:
    enabled: true
    domainName: k8s.stfc.skao.int
  storageClass: nfss1
  dataProduct:
    pvc:
      name: test-pvc
      create: true
      storage: 20Gi

gateway:
  enabled: true

postgresql:
  enabled: true
  initialise: true

keycloak:
  enabled: true

pgweb:
  enabled: true