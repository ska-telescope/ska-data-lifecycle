# Example values to point to an existing Kubernetes Secret containing the authentication details
# used by the PostgREST service to connect to PostgreSQL
postgrest:
  db_auth_secret:
    create: false
    name: the-name-of-the-existing-secret
